NODE_ENV=development
PORT=5000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173,http://127.0.0.1:5173,https://tech-store-ashen-three.vercel.app
# Render: include your exact Vercel URL(s), comma-separated, no trailing slash
RATE_LIMIT_MAX=1000

# Production (Render): set these in Render Dashboard → Environment
